Installation:

    git clone git@github.com:itsdtr/dotvim.git
    cd ~/.vim
    git submodule init
    git submodule update

Create symlink:

    ln -s ~/.vim/vimrc ~/.vimrc

Supporting Software:

    pip install jedi
